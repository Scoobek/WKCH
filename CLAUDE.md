# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

WKCH is a blog for sighthounds in Poland. The project is in early setup — only a `package.json` exists so far; the stack, framework, and structure are not yet defined.

## Commands

No build, lint, or test commands are configured yet. Update this file once the stack is chosen and scripts are added to `package.json`.
